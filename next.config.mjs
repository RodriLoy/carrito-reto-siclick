/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com", "images.unsplash.com"], // Agrega el dominio de la imagen aquí
  },
};

export default nextConfig;
